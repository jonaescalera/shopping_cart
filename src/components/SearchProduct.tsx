const SearchProduct = () => {
  return (
    <div>
      <input
        className="searchBar"
        type="text"
        placeholder="Search products, brands..."
      />
    </div>
  );
};

export default SearchProduct;
